apiVersion: v1
kind: Service
metadata:
  name: rancher-ui
  namespace: cattle-system
spec:
  type: LoadBalancer
  selector:
    app: rancher
  ports:
    - name: http-ui
      protocol: TCP
      port: 7010
      targetPort: 80
